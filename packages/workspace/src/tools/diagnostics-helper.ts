import { RunDiagnostics } from "./run-diagnostics";

export const DIAGNOSTICS_HELPER_MESSAGE = `When you're done with your current set of changes to this file, you should call the ${RunDiagnostics.name} tool to check for any new errors.`;
